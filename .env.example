# TEMPLATE ONLY — committed to git. NEVER put the real key here.
# Put your real Groq key in a file named `.env` (gitignored). For production,
# set GROQ_API_KEY in Netlify -> Site configuration -> Environment variables.
GROQ_API_KEY=your_groq_api_key_here
